# Chat Application

A simple chat application built with Flask and SQL Server.

## Features
- User authentication
- Real-time messaging
- Online/Offline status
- Message read status
- Chat history

## Setup
1. Install requirements: `pip install -r requirements.txt`
2. Configure database connection in config.py
3. Run SQL scripts in data.sql
4. Start server: `python app.py`
